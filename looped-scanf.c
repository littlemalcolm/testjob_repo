#include <stdio.h>
#include <stdlib.h>

/** A function that asks the user a question.
  * gets a feedback from user.
  * checks the feedback with certain system criteria(s)
  * and gives the user the right response.
  */
int main(void)
{
	int p, r, f;



	for(p = 0; p <= 5; p++)
	{
		for(f = 0; f <= 13; f++)
		{
			printf("First nested loop works corretly.\n");
		}
	}
}
