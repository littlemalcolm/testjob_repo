#include "main.h"

/**
  * add - adds two integers and returns the result.
  * Description: takes two integer arguments a and b.
  * @a: first argument value to be checked.
  * @b: second argument value to be checked.
  *
  * return: Returns (a + b).
  */

int add(int a, int b)
{
	int add = a + b;

	return (add);
}
